/**
 * Resource Hints Component
 * Adds resource hints to the document head for better performance.
 * Preconnect/preload are already in [locale]/layout.tsx <head> and preloading.ts;
 * this component is kept for future route-specific hints and returns null.
 */
'use client';

export function ResourceHints() {
  return null;
}
