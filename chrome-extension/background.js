/**
 * ImmoAssist Extension - Background service worker
 * Handles optional background logic; popup and content script communicate directly.
 */

chrome.runtime.onInstalled.addListener(() => {
  // Optional: set default options or open onboarding
});
